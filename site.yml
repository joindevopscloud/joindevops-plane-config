---
- hosts: localhost
  connection: local
  become: true
  roles:
    - common
    - data-mount          # mount /data first — Docker's data-root lives on it
    - docker
    - plane-bootstrap
    - nginx
